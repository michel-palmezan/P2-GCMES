[tox]
envlist = py39
skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
    psycopg2
    python-dotenv
    flask
commands = pytest --cov=. --cov-report=xml --cov-config=tox.ini --cov-branch
